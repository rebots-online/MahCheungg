/**
 * Enum representing the position of a claimed tile in a chow
 */
export enum ChowPosition {
  LOWER = 'lower',   // The claimed tile is the lowest in the sequence
  MIDDLE = 'middle', // The claimed tile is in the middle of the sequence
  UPPER = 'upper'    // The claimed tile is the highest in the sequence
}
