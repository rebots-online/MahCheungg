/**
 * Enum representing the different types of tile sets
 */
export enum TileSetType {
  CHOW = 'chow',   // Three sequential tiles of the same suit
  PUNG = 'pung',   // Three identical tiles
  KONG = 'kong'    // Four identical tiles
}
